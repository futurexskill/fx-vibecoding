# Production Deployment Configuration
# Update these values for your specific AWS environment

# S3 Configuration
S3_BUCKET="fx-stock-dashboard-prod"
S3_REGION="us-east-1"

# CloudFront Configuration (optional)
CLOUDFRONT_DISTRIBUTION_ID=""  # Add your CloudFront distribution ID if you have one

# Lambda Functions
LAMBDA_FUNCTIONS=("stock-data-processor" "stock-api")
LAMBDA_REGION="us-east-1"

# API Gateway (for reference)
API_GATEWAY_ID="7kiye42406"
API_GATEWAY_STAGE="prod"

# Environment-specific settings
NODE_ENV="production"
BUILD_DIRECTORY="dist"

# Notification settings (optional)
SLACK_WEBHOOK_URL=""           # Add Slack webhook for deployment notifications
EMAIL_NOTIFICATION=""          # Add email for deployment notifications

# Backup settings
BACKUP_BEFORE_DEPLOY="false"   # Set to "true" to backup before deployment
BACKUP_S3_BUCKET=""           # S3 bucket for backups

# Health check URLs (after deployment)
HEALTH_CHECK_URLS=(
    "https://7kiye42406.execute-api.us-east-1.amazonaws.com/prod/health"
    # Add more health check URLs as needed
)

# Custom deployment hooks (scripts to run)
PRE_DEPLOYMENT_SCRIPT=""       # Script to run before deployment
POST_DEPLOYMENT_SCRIPT=""      # Script to run after deployment

# Security settings
REQUIRE_MFA="false"            # Set to "true" to require MFA for production deploys
ALLOWED_DEPLOYERS=""           # Comma-separated list of allowed deployer emails

# Monitoring
ENABLE_DEPLOYMENT_TRACKING="true"  # Track deployments in analytics
DEPLOYMENT_LOG_LEVEL="info"        # error, warn, info, debug